runtime: custom
env: flex

manual_scaling:
  instances: 1

env_variables:
  # Read the values from the "from-repo" and "to-repo" project metadata values:
  # https://console.cloud.google.com/compute/metadata
  REPOS: '[{"ID":"default","From":"metadata:from-repo","To":"metadata:to-repo","HTTPCookie":"..."}]'
